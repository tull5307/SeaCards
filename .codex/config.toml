# Project-scoped Codex config (loaded when trusted)
# See: Codex project config files (.codex/config.toml)

approval_policy = "on-request"
sandbox_mode    = "workspace-write"

# Keep network off by default; enable only if you want Codex to run installs itself.
[sandbox_workspace_write]
network_access = false
